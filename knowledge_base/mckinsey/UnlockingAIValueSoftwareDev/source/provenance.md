# Provenance — Unlocking the value of AI in software development

- Canonical URL: https://www.mckinsey.com/industries/technology-media-and-telecommunications/our-insights/unlocking-the-value-of-ai-in-software-development
- Retrieval date: 2026-09-14
- Route that worked: **route 4 — reconstruction from multiple search-result extracts**. All claims in `full.md` are traceable to indexed extracts of the canonical McKinsey URL and its mckinsey.com.br mirror; joinery and inferred structure are flagged `[unverified]`.

## Routes tried

1. `curl --http1.1` with browser User-Agent (Chrome 126 on macOS) against the canonical URL — **failed**: operation timed out after 40 s with 0 bytes received (also tried against the mckinsey.com.br mirror — same timeout).
2. WebFetch tool of the canonical URL — **failed**: request timed out. Also tried on the mckinsey.com.br mirror and on a Sipoch mirror page — mirror timed out; Sipoch returned only site shell (JavaScript-rendered, no article text).
3. Reader proxies — `https://r.jina.ai/<url>` — **failed**: target returned 403 Forbidden (Akamai/EdgeSuite access-denied page). Google cache no longer available as a product.
4. Search-result extracts — **worked**: web search returned extensive verbatim highlights from the canonical URL and mirror covering the thesis, methodology sidebar, performance numbers, both shifts, the Cursor case study, all three enablers with statistics and both executive quotes, and the closing passage. Reassembled into `source/full.md` with `[unverified]` flags on structure/transition inferences.
