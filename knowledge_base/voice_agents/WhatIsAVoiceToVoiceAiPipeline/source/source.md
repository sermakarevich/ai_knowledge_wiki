> PDF location: https://www.youtube.com/watch?v=fma4F37o8EY (no source.pdf fetched; youtube source — see Source field below)
# What is a Voice to Voice AI Pipeline? | Reduce Latency & Add Emotion to Voice Agents
Source: https://www.youtube.com/watch?v=fma4F37o8EY
Kind: youtube
Fetched: 2026-09-22T07:44:59.220263+00:00
Tool: yt

Does your voice AI agent has emotions involved into that? When we talk to human to human, there is a connection which is emotionally collected. The way that human speak the other human understand is basically the tone, the emotions, what user want to say. We have seen all the voice AI agent being converted the voice into text and text into a game voice. So that's the pipeline that we have seen. But that is lacking the emotion what user want to say in their sentence. Hi myself Nishant. I'm CTO and co-founder at cudist. In this video I will walk you through the voice pipeline which is a voiceto voice pipeline. In previous video we have seen like voicetooice pipeline which has a voice to text text to voice conversation. In this video I will walk you through voice to directly voice conversation. Let's get started. Okay great. Let's get started. So so far in the last video we have seen if you are building a voice a pipeline then the first module is a in the voice input that goes to speech to text then it goes to LLM and then it goes to text to speech and then basically again voices and output comes in and that is comes as a speaker as an output. So a user can hear that right. So this is the whole pipeline that we have seen. Now what is the problem? The problem statement that I have seen in this is majially. So the major problem that I have seen is whenever a voice that comes in and that it converts into text we lose the emotions. So let's say if I speaking with the sid or anger the biggest problem is if it's converted into text and then llm don't know what exactly the emotion looks like. So the first solution that comes into mind okay keep a emotions detection library but the emotion detection library again will work on the transcription but users voice has the tone the the exactly specific I would say the emotion they have which cannot be detected I would say by transcription. So that's where the problem starts. Now the solution, how about this? Right? So let's say if voice comes as an input and it fits to L&M as a voice and then output also a voice. So this is called a voicetooice pipeline. Make sense? So this is exactly going to solve the problem of latency and it is going to be the solution for I would say the emotion understanding. So exactly we have a narrative of what LLM has to respond and it and in which specific emotion as an output as well. So it's it's a more of a nature you know how one user speaks to another how a human to human understanding is like okay if I'm a then the person understand the feeling of the state over here that was being converted into text. So there was no emotion being passed from user to LLM and that was the reason that it feels a bit of robotics as well and then there is a latency as well right to solve this particular problem there is another pipeline called voicetooice pipeline so I will walk you through how it works and what are the models that is being integrated as part of the voice to voice pipeline right okay perfect let's so this time I have made a specific uh diagram so at least you can understand. So let's consider so there are total four modules in this step one is encoder the output is walkoder consider like this part is switch to text just for sake of understanding and this workoder is text to switch previously this was converted into text now anymore it is not going to be converted into text so I will walk you through exactly the pipeline modules right so consider like a user speaks Hi, I want to talk to Mark. Let's say the word is in dang. Hey, I want to talk to Mark. In previously it was converted into text. So, NLM doesn't understand the emotions. In this case, what we are trying to do over here is like let's say this is a encoder. What encoder does is basically it has a speak to text uh kind of functionality. What it does is it is not converting into text. What it does instead is let's say a voice comes in that in it converted into vectors. This vectors are a voice vectors. It has a emotions and it has a meaning. What I mean meaning that and what exactly user is trying to say and the emotions as well. the tone what kind of tone and speed it is coming up with. So that's the what exactly the encoder does. So what it basically does is basically converted the particular row audio into data vectors. So these are data vectors that comes as an output. So this data vectors goes into uh adapters. Normally this data vectors can go directly into LLM as well. Now question is this the same alin that we can use that we have used in open AI that you have used into uh voice to text and text to voice pipeline no this is specific different kind of llm what I mean by that the llms so it is basically this is an lls which can accept voice vector as an input normally like open AI models let's say GPT models they don't we don't have a full control because we don't have a weight so we cannot change as an input what goes as an input why because we don't have a modification options so that's why what we can do is we can use llama Omnimodels. Llama omnimodels is specifically voicetooice models. You can input that as a voice vectors. the data vectors and then it goes to LLM and then LLM's basically convert that gives output as a u again a text or I would say a voice vectors or it gives us a voice vector whatever we need and then those voice vectors goes to walker so walker converts those w vectors voice vectors into speech right so let me again explain you and what is the use of uh over here the length modality adapters as well. So over here the the speech basically comes as an input to speech comes into speech encoder that speech converted into vectors as this vectors are normally of the high in the length let's say longer in the size. So this modality L basically convert that into LLM compatible length. So consider like it's a larger length. It converts into small chunk of lens and then feed that into large language model. This large language models like llama omni make sure we cannot use open AI GPT models currently as of today. In future they might accept the voice vector as an input as well. Yeah. And then as the next output it consider again as a voice vectors and this voice vectors converted into speech. Yeah. So what is the benefit of this? So the benefit of this first is low latency. The second is emotions being passed through the pipeline. Now obvious question comes into picture does this LLM can accept our prompt? Yes. So llm input is voice vector plus prompt that's goes as an input to particular this large language model right so I will again kind of say voice comes as an input to encoder it converts the length into specific length that large language model accept inside the large language model this voice vectors in the prompt voice as an input it gives output as a voice vectors in walk order basically convert that voice vectors into speech which user can hear. Yeah. Perfect. So that's how exactly it works. Now the the limitations right limitations is less accurate in tune calling. The problem statement is in the real time world in real world still there is text to ST LLM text to speech. This is the pipeline that has been deployed in almost all the use case in the real world that I have seen so far. It can be implemented with the voice to voice but as I mentioned like when there is a tool calling comes into picture there is a low accuracy and also you have a less control of the prompt as well. Yeah. So perfect. So that's the pros and cons and that's how the voice AI pipeline works. If you do have any question um feel free to ask. Uh thank you so much. Thanks for watching this video so far. Hope you get the insight about voice to voice pipeline. when to use voice to voice AI pipeline and when not to use it. If you are building a voice AI pipeline or voice infrastructure and if you need assistance or if you need a development team that can help you to build that particular infrastructure or get involved into your current team, we are more than happy to collaborate with you. I have attached my Kenly link in this video. Feel free to reach out and have a consultation call. Thank you.

