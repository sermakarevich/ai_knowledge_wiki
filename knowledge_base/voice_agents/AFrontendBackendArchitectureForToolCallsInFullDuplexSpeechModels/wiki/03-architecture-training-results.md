[[../index|Wiki]] | [[../summary|Summary]] | [[../digest|Digest]]
# Architecture, Training and Results: STT Frontend, LangGraph Backend, Tool-Call Delegation
**In one sentence:** The paper trains a duplex speech-to-text frontend to emit a delegation token pair (`<tc bos>`/`<tc eos>`) in the agent-text channel when a tool is needed, routes the endpointed transcript to a LangGraph ReAct backend that runs multi-round tool calls, and prefills the backend's natural-language answer back into the frontend for spoken generation.
## Key points
- Frontend is a duplex STT model with three input streams (user speech, user transcript, agent text): a 600M-parameter Parakeet streaming encoder plus NVIDIA Nemotron-Nano-9B-v2-Base backbone, with a separate ASR embedding/prediction head sharing the backbone and a single decoding pass producing user and agent text jointly.
- Delegation mechanism: if user speech needs a tool (e.g. "what is the weather in New York?"), the agent-text channel fires `<tc bos>` (replacing regular `<agent bos>`, usually 320 ms after user-turn end), then a ~1 s filler, then `<tc eos>`; the backend answer is enclosed `<pf bos>`/`<pf eos>` and prefilled ~1 s after `<tc eos>`, which the frontend learns to continue from `<agent bos>`.
- At inference the `<user eos>`-endpointed transcript is sent to the backend, the backend's natural-language response is injected into the agent-text channel, and agent output is suppressed with pad tokens during the call; pad/silence regions with prefilled text are excluded from loss in training.
- Backend (Sec. 3.2) is LangGraph [33], a tool-augmented ReAct agent [16]: a state machine over a running message list with an instruction-following LLM agent node, a tools node, a conditional edge routing to tool execution whenever calls are emitted, and a thread-keyed checkpointer for multi-turn state; invalid tool-call syntax or plain-text replies are returned directly as prefill, so the larger backend falls back to natural-language explanations when the frontend misfires.
- Training (Sec. 4) is pretrain + SFT with ~530k hours pretraining, 111k hours SFT, ~16k hours ASR transcription, and 8.5k hours multi-turn tool-call data; text dialogues are generated by Nemotron 3 Nano / Gemma-4-31B-IT / Qwen3.5-397B-A17B then LLM-judge filtered, audiofied via filtering (drop math/code-heavy), VoiceChat-TTS, and Parakeet-tdt-0.6b-v2 WER/CER filtering, plus domain data (airline/retail) from two Qwen3.5-235B-A22B LLMs with failures discarded and user turns via Chatterbox.
- Frontend tool-call token recall is 97.2% (Simple), 92.0% (Multiple), 95.0% (Parallel), 93.5% (Parallel-Multiple); Ours-30B-extASR averages 74.6% BFCL AST vs 73.0% intASR, with Parallel-Multiple 55.1% → 61.1% from more accurate external ASR.
- On Full-Duplex-Bench v3 (100 real-human scenarios, 12 speakers) Res-Q is ~54–67% (larger Qwen3-235B-A22B backend better), turn-taking 100%, with high filler rate by design ("Let me pull that up") and high interruption rate from backchannels ("Okay, I am here.") while still listening; adding tool-call training only slightly degrades turn-taking Pr/Rec (85/94→82/91), ASR WER (10.80%→11.47%), and CommonEval (2.87→2.36).
---
## 3. Architecture overview and related contrast
**Covers:** Sec. 3 intro + Fig. 1 caption/context

Proposed system (Fig. 1: "The proposed frontend-backend system for a duplex speech-to-text model with tool-call capability"): "we propose a straightforward approach to predict a delegation token in the agent text channel to assign tool calls to a backend. The backend executes multiple rounds of tool calls, and then prefills the response text to the frontend as context for further generation. The proposed frontend-backend framework is modular and does not need significant architecture changes for the frontend."

Contrast stated in chunk:
- "Commercial systems [30, 1, 41] expose tool calls inside an end-to-end voice API, but their architectures are not disclosed."
- "KAME [26] runs a Moshi-style frontend and conditions its output on 'oracle' tokens streamed from a backend LLM updated every 100–500 ms; the frontend is explicitly trained to anchor its speech on these oracle tokens."
- "MoshiRAG [27] augments Moshi with retrieval, injecting retrieved context into the inner-monologue stream."
- "Thinking Machines adopts an interaction-background style system to delegate deep reasoning, tool calls, or longer-horizon jobs to the background model [28]."
- "Recently, Qwen-audio-agent [29] is proposed as a realtime voice model with backend agents which can execute various tasks while the frontend is in a conversation with the user. However, in [28, 29], it is unclear how the delegation signal is designed or backend results are integrated into the frontend duplex model."

## 3.1. Speech-to-text frontend and delegation tokens
**Covers:** Sec. 3.1 + training-sequence example

- Inputs: user speech, user transcript, agent text. "User speech is encoded by a 600M-parameter Parakeet streaming speech encoder [43] and fed to a backbone LLM (NVIDIA Nemotron-Nano-9B-v2-Base [44])."
- "The streaming ASR head consists of a separate embedding layer and prediction head, and share the same LLM backbone as the agent text head. A single decoding pass jointly produces both user and agent text."
- Delegation training: "we train the frontend model to fire a tool-call token <tc bos> in the agent text channel if the user speech naturally requires a tool call (e.g., 'what is the weather in New York?'). We then train the model to first generate a short filler followed by the special token <tc eos>."
- Labeling/timing: "if a user query leads to a tool call, we label the following agent turn as a tool-call turn, along with the final natural-language tool-call response. We first place a <tc bos> token to replace a regular <agent bos> token, which is usually 320 ms after the end of the user turn (as in [42, 6, 31]), then followed by the filler (~1s duration) and then <tc eos>."
- Prefill timing: "The natural-language tool response is then enclosed by <pf bos> and <pf eos> and prefilled into the agent text channel around 1s after the <tc eos> to simulate the tool call execution time, and the frontend model is trained to generate the exact prefilled text starting with <agent bos>."
- Loss masking: "During training, we insert pad tokens to the ASR channel and silence to speech regions as inputs with prefilled agent text, and these regions are not used for loss computation."
- Verbatim example sequence:
  - "User: What is the weather in New York?"
  - "Agent: <pad>320 ms [ <tc bos> Give me a moment. <pad>... <tc eos> ]1 s <pad>1 s <pf bos> The weather in New York is 72 degrees <pf eos> <agent bos> The weather in New York is 72 degrees <pad>..."
- Inference: "once the frontend fires the tool-call detection token, the user transcript, endpointed by <user eos>, is sent to the backend to complete the tool-call request. The resulting natural-language response is then injected into the agent-text channel before further generation. During the function call, we suppress the agent output by inserting pad tokens in the agent text channel."

## 3.2. Backend agent
**Covers:** Sec. 3.2

- "We use LangGraph [33], a tool-augmented ReAct agent [16], as our backend. Our graph is a state machine over a running message list, comprising an agent node that employs an instruction-following LLM and a tools node that executes the model's tool calls."
- "The two nodes are linked by a conditional edge that routes to tool execution whenever the model emits calls and terminates the turn once the turn contains no tool calls."
- "When the user query requires a tool call, e.g., 'what is the weather in New York?', the user ASR transcript is sent to the graph as a message, and the agent is invoked to call tools to respond."
- "Multi-turn context is maintained automatically by a thread-keyed checkpointer that persists and restores the accumulating conversation state across turns, so each turn only takes the current user ASR transcript as input."
- Fallback: "If the backend generates a syntactically invalid tool-call request or a regular text response, we return the text directly to the frontend as prefill. This means the frontend may have misfired a tool-call request, while the larger backend reliably falls back to natural-language explanations for irrelevant queries."

## 4. Training and inference data
**Covers:** Sec. 4

- "For the frontend duplex STT model, our training includes a pretraining stage followed by supervised fine-tuning (SFT) [42]."
- Generation: "we use various LLMs (e.g., Nemotron 3 Nano [45], Gemma-4-31B-IT [46], and Qwen3.5-397B-A17B [47]) to generate user, agent, tool call and tool response turns in text, and use LLM judges to filter conversations with inconsistent turns or incorrect tool invocations."
- Audio pipeline: "filtering (e.g., removing math-heavy and code-heavy conversations not suitable for audio), TTS (VoiceChat-TTS [32] with diverse voices), ASR (Parakeet-tdt-0.6b-v2 [48]) based quality filtering using WER/CER."
- Coverage: "variants targeting voice-agent decisions about when to call a tool, when to ask follow-up questions, and when not to call an unavailable or inappropriate tool, similar to When2Call [49] but with multi-turn speech user input" plus "realistic multi-turn interactions with backchannels, pauses, and interruptions."
- Domain data: "simulated airline and retail databases) with entities distinct from evaluation. We generate conversation and tool-calling trajectories by having two text-only LLMs interact with each other (both are Qwen3.5-235B-A22B [50])... Samples that failed to complete the task are discarded, and we synthesize the user turns into speech using Chatterbox [51]."
- Totals: "around 530k hours of pretraining data, 111k hours of SFT data, around 16k hours of ASR transcription data, and 8.5k hours of multi-turn conversation data with tool calls."
- Prompt generalization: "we randomly choose either the full tool definition as the system prompt or a partial prompt with only function name and description for generalization."

## 5. Results
**Covers:** Sec. 5.1–5.3 + Tables 1–7

Setup: audio BFCL single-turn (Simple, Multiple, Parallel, Parallel-Multiple, Irrelevance) with AST score "which tolerates argument order, spacing and formatting differences, and default parameters"; backends Qwen2.5-7B and Qwen3-30B-A3B with internal vs external ASR [43]; FDB3 = 100 scenarios, 12 speakers, real human recordings, mock APIs, GPT-4o judge; EVA-Bench = 213 scenarios (airline, ITSM, medical HR), GPT-5.2 caller, Chatterbox-TTS, Triton server, GPT-5.2 judge, agent text returned directly.

Table 1. BFCL AST accuracy (%) and irrelevance (excerpted exact):

| Model | Simple | Multiple | Parallel | Para. Multi. | Irrelev. | Avg |
|---|---|---|---|---|---|---|
| GPT-realtime | 82.9 | 83.6 | 72.5 | 74.0 | 90.8 | 80.8 |
| UV0.6-8B | 61.4 | 63.5 | 48.5 | 43.0 | 0.0 | 43.3 |
| UV0.6-32B | 78.9 | 81.1 | 75.2 | 65.4 | 82.5 | 76.6 |
| Ours-7B-intASR | 80.2 | 79.6 | 69.5 | 52.7 | 76.7 | 71.7 |
| Ours-30B-intASR | 79.4 | 78.3 | 71.1 | 55.1 | 81.2 | 73.0 |
| Ours-30B-extASR | 80.1 | 78.7 | 72.0 | 61.1 | 81.2 | 74.6 |

Stated findings: "our 30B backend setup performs close to GPT-realtime on some subsets, however the largest gap is on Parallel-Multiple, followed by Irrelevance"; extASR improves avg 73.0%→74.6% with biggest gain Parallel-Multiple 55.1%→61.1%; "Our frontend achieves recall of 97.2%, 92.0%, 95.0%, and 93.5% on Simple, Multiple, Parallel, and Parallel Multiple, respectively"; "7B backend substantially outperforms Ultravox-v0.6 Llama-3.1-8B in average score (71.7 vs. 43.3)"; UV0.6-8B irrelevance 0.0 "because it invokes the single offered function on all 240 prompts."

Table 2. FDB3 (Tool-acc / Arg-acc / Pass@1 / Res-Q / TT / Inter / Filler):

| Model | Tool-acc | Arg-acc | Pass@1 | Res-Q | TT | Inter | Filler |
|---|---|---|---|---|---|---|---|
| GPT-realtime-mini | 77.4 | 58.2 | 51.0 | 62.0 | 93.0 | 14.2 | 17.2 |
| GPT-realtime | 88.7 | 71.6 | 61.0 | 74.7 | 95.0 | 13.5 | 12.1 |
| Gemini-3.5 Flash | 97.0 | 72.5 | 65.0 | 88.0 | – | – | 1.0* |
| UV0.6-8B | 59.1 | 39.0 | 11.0 | 19.0 | – | – | 2.0* |
| UV0.6-32B | 84.3 | 52.2 | 45.0 | 71.0 | – | – | 12.0* |
| Ours-30B-extASR | 74.6 | 52.8 | 44.0 | 54.0 | 100.0 | 54.0 | 84.8 |
| Ours-235B-extASR | 71.7 | 55.2 | 48.0 | 67.0 | 100.0 | 51.0 | 83.3 |

Stated: "our systems fulfill the user's request (Res-Q) at around 54%-67%. A larger backend (Qwen3-235B-A22B) performs better"; "Our model achieves 100% turn-taking. Our high Filler rate is by design: the frontend emits a short hold phrase ('Let me pull that up')"; "Our high Inter rate has a different cause: the frontend typically responds at pauses during the user's disfluency with a backchannel (e.g., 'Okay, I am here.') before any tool call. Because it keeps listening while speaking, the eventual tool call usually still sees the complete user request (Res-Q up to 67.0)."

EVA-Bench (Table 3 excerpt): GPT-5.2 text topline task 78.4%; Ours-30B-extASR EVA-A/EVA-X/Task/Faith 32.4/63.5/40.4/24.4, TT 151 ms; Ours-235B-extASR 46.6/74.7/57.3/35.9, TT 352 ms; Qwen3-235B-A22B "achieves higher task completion in the airline domain than GPT realtime2 (72% vs. 54%) but underperforms in ITSM (56.3% vs. 75%) and Medical HR (49.4% vs. 69.9%)", needing "a chain of up to 6–8 successful tool calls."

Turn-taking/intelligence/ASR/FDB-v1 (Tables 4–7 excerpt):
- Internal set: Baseline (no TC) Pr/Rec 85/94, Lat 423 ms, BI 100%/403 ms; Ours 82/91, 438 ms, 99%/395 ms.
- OpenbookQA/AE/CE: Baseline 65.0%/3.54/2.87; Ours 64.7%/3.61/2.36.
- ASR WER avg: Baseline 10.80%, Ours 11.47% ("presumably because the added tool-call training data are mostly synthetic").
- FDB-v1: Baseline smooth-TT TOR 93%, 221 ms, pause TOR 53.2%, interruption TOR 95.5%, GPT 3.94, lat 590 ms; Ours 100%, 92 ms, 68.2%, 89.5%, 4.04, 349 ms — "more responsive, with a higher TOR rate for smooth TT and lower latency, but it also performs worse on pause handling."

## 6. Conclusion
**Covers:** Sec. 6

"We propose a frontend-backend architecture that enables a full-duplex model to perform tool calls and agentic tasks. Our frontend model is trained to emit a tool-call token that signals the need for backend delegation, and the tool-response text from the backend is prefilled to the frontend as context for the further conversation. Our results show that this architecture works with different backend LLMs and achieves competitive results on tool-calling and voice-agent benchmarks."

**Covers:** Sec. 3–6 (frontend STT + Sec. 3.2 LangGraph backend + Sec. 4 training/inference + Sec. 5 BFCL/FDB3/EVA/turn-taking results + Sec. 6 conclusion); chunk slug 03-pooling-3-2-backend-agent-turn-1-silence.
