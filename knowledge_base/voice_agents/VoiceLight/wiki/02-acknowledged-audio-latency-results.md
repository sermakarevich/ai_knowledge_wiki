> [[../index|Wiki]] | [[../summary|Summary]] | [[../digest|Digest]]
# Acknowledged Audio and Latency Results
**In one sentence:** Voice-Light treats only browser-acknowledged audio as durable conversation context and reports system-level latency measurements in which speculation means privately preparing downstream text and PCM before a final turn commitment.
## Key points
- Latency results are system measurements and are not directly comparable with model-internal latency reported for end-to-end architectures.
- Speculation here means privately preparing downstream text and PCM before a final turn commitment, distinct from prior work combining local and remote detectors.
- The browser streams 16 kHz microphone PCM over one WebSocket and acknowledges rendered audio ranges; generated assistant text becomes durable context only to the extent acknowledgments show it was audible.
- Silero provides the earliest acoustic onset signal, while a persistent Nemotron worker produces streaming transcripts and exposes selected encoder layers to the causal adapter without loading a second speech backbone.
- Qwen generates speech and typed calls while Kyutai TTS synthesizes only text released by the controller, which combines ASR signals with transcript revisions, playback state, and conservative deadlines.
- The Voice-Light tool branch addresses a narrower spoken protocol — short audible bridges, causally ordered call/result records, sequential calls, and suppression of protocol markup — and its shared-generator holdout measures acquisition of that protocol, not general tool use or factual retrieval.
- Spoken tool-use supervision was generated by a pinned Qwen3.6-27B-FP8 teacher as provider-neutral records, yielding 3,999 conversations, 11,810 user messages, 15,308 assistant messages, and 3,498 structured calls across search, calculate, and get time.
- The turn-taking branch synthesizes only user audio from typed interaction plans distinguishing genuine completion, HOLD pause with continuation, non-floor feedback, post-yield response, and deliberate interruption, compiling runtime assistant-speaking probability as a causal state curve.
---
## Latency comparability and speculation
**Covers:** §2 related-work tail (acknowledged audio; latency; speculative end-turn detection)

- Durable history rule: generated assistant text becomes durable conversation context only to the extent that browser acknowledgments show it was audible.
- Latency scope (verbatim): "Its latency results are system measurements and are not directly comparable with model-internal latency reported for end-to-end architectures."
- Speculation definition (verbatim): "here, speculation means privately preparing downstream text and PCM before a final turn commitment."
- Prior reference: speculative end-turn detection has also combined local and remote detectors [9].

## System overview
**Covers:** §3 System overview + Figure 1 data path

- Figure 1 shows the deployed data path: the browser streams 16 kHz microphone PCM over one WebSocket and acknowledges rendered audio ranges.
- Silero provides the earliest acoustic onset signal.
- A persistent Nemotron worker produces streaming transcripts and exposes selected encoder layers to the causal adapter without loading a second speech backbone.
- The controller combines those signals with transcript revisions, playback state, and conservative deadlines.
- Qwen generates speech and typed calls; Kyutai TTS [17] synthesizes only text released by the controller.
- Four invariants define the system:
  1. All learned turn features are causal and share the ASR stream.
  2. Speech onset may duck or pause playback immediately, but only stronger lexical, learned, or timeout evidence commits cancellation.
  3. Speculative text and PCM carry generation identifiers and remain private until transcript and turn checks promote them.
  4. Generated assistant text becomes durable conversation context only to the extent that browser acknowledgments show it was audible.

## Training data branches
**Covers:** §4 Training data opening + Figure 2

- The project produced two model-training branches: a language-model branch generating typed spoken-tool conversations for a Qwen adapter, and a turn-taking branch combining synthetic interaction timelines with automatically prepared human conversations for a causal speech adapter.
- Figure 2 shows where the branches remain separate and where they meet the deployed system: pinned 27B teacher plus typed scenarios → 3,999 canonical tool-use conversations → Qwen3-1.7B LoRA experiment; typed interaction plans plus synthesized user speech → dense causal frames at 80 ms; licensed two-speaker human conversations plus ASR/VAD/alignment/regions/review → frozen Nemotron causal adapter → locked evaluation and deployed hybrid.

## Synthetic spoken tool use
**Covers:** §4.1 Synthetic spoken tool use

- Spoken tool use was treated as a data and protocol problem rather than a prompt-only feature.
- A pinned Qwen3.6-27B-FP8 teacher generated provider-neutral records for natural conversation, drafting, brainstorming, search, calculation, time queries, sequential calls, and correction or recovery.

| Corpus count | Value |
|---|---|
| Conversations | 3,999 |
| User messages | 11,810 |
| Assistant messages | 15,308 |
| Structured calls (search, calculate, get time) | 3,498 |

- The corpus is available through the project collection on Hugging Face [27].
- Prior supervision context: Toolformer and ToolLLM construct supervision for API selection and multi-step execution [10, 11].
- Voice-Light scope: short audible bridges, causally ordered call/result records, sequential calls, and suppression of protocol markup; its shared-generator holdout measures acquisition of that protocol, not general tool use or factual retrieval.
- Generation used a causal conversation state machine: the teacher produced a user turn and then either assistant speech or speech followed by a typed call; the controller executed or synthesized the result, appended the linked call and result, and only then requested the next assistant step.
- Calculator and time results were deterministic; synthetic search results came from the teacher, not the live web, so the corpus teaches call ordering and grounding to supplied text, not factual retrieval quality.
- The canonical JSONL is semantic rather than tokenized: it stores runtime tool definitions, audible assistant text, typed calls, stable identifiers, outcomes, later turns, split metadata, provenance, and hashes.
- The run pins the teacher revision, prompt revision, source commit, scenario seeds, quantization, and time anchor.
- Structural checks retained schema validity, causal call/result order, deterministic tool checks, spoken-length limits, and protocol-leak detection; subjective teacher-based rejection was disabled after manual review found its false positives counterproductive.

## Synthetic conversational turn-taking audio
**Covers:** §4.2 Synthetic conversational turn-taking audio (partial in chunk)

- The second generator models coherent conversation timelines rather than isolated completion utterances.
- Typed plans distinguish five semantic conditions: genuine user completion, a HOLD pause followed by continuation, non-floor feedback during assistant speech, a response after the assistant yields, and deliberate interruption; the plan also contains ordinary event-free user, assistant, and silence spans.
- Only user audio is synthesized; virtual assistant text keeps the scenario coherent and estimates assistant duration, but assistant waveform never becomes an input artifact.
- The runtime-relevant assistant-speaking probability is compiled as a causal state curve with ramps, dips, and bounded noise.

**Covers:** acknowledged-audio history rule and reported latency results; §§2-tail–4.2 (pp. 1–2), arXiv:2609.20995v1 [cs.SD] 17 Sep 2026
