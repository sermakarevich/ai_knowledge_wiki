This figure is a horizontal point-estimate plot (with error bars) quantifying the effect of adding *in‑skill* in‑context examples on Pass@2, expressed as the change in percentage points (pp): **Contribution = full − (−X)**. The vertical axis lists four models plus a pooled aggregate (DeepSeek, Qwen, GPT‑5.1, Sonnet, "All (pooled)"). The horizontal axis runs from about −25 to +12 pp, with a dashed reference line at 0 and shaded/annotated regions labeling negative values as "examples hurt" (left) and positive values as "examples helped" (right).

Per‑model trends: DeepSeek shows the largest positive contribution (point estimate ≈ +8 to +9 pp, bar ≈ +5 to +12). Qwen is modestly positive (≈ +3 to +4 pp, bar ≈ +2 to +5). GPT‑5.1 is essentially null (point estimate near 0, bar spanning roughly −2 to +2, i.e., straddling the zero line). Sonnet is strongly negative (point estimate ≈ −15 pp, bar ≈ −24 to −8, entirely on the "hurt" side). The pooled estimate is slightly negative (≈ −1 pp, bar ≈ −5 to +3), straddling zero but tilted toward the hurt side.

**Takeaway:** In‑skill examples do not have a uniform benefit. They help DeepSeek and Qwen and are roughly neutral for GPT‑5.1, but they materially *degrade* Sonnet's Pass@2 (on the order of a 15 pp drop). Consequently, the aggregate (pooled) effect is near zero to slightly negative, driven by Sonnet's outlier penalty — so the average masks a meaningful model‑specific split, and the choice to use in‑skill examples should be made per model rather than assumed universally beneficial.