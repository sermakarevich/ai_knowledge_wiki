# Extract: 04-appendices-worked-examples

## Context is tight — read ONLY these files, nothing else

- Input chunk text: `/Users/sergii/.kb/papers/SignalOrNoiseAgentSkills/source/chunks/04.txt`
- Figure description (already generated by a vision model, plain text):
  - `/Users/sergii/.kb/papers/SignalOrNoiseAgentSkills/wiki/images/09-description.md`

Do NOT read this task's own fleet artifacts/log/event files (`~/.fleet/tasks/<id>/...`, `events.jsonl`,
`task.json`, `PLAN_AND_STATUS.md`, `KNOWLEDGE.md`), and do NOT read sibling wiki pages "for style/convention
reference" — the format contract below is the only convention you need. If this is a retry, do not diagnose
the prior failure by reading logs; just re-read the chunk and write directly.

## Task

Read the chunk text (extracted from an academic paper, "Signal or Noise? A Benchmark Study of Agent Skills
in Web Development", arXiv:2608.23067). This chunk covers the paper's appendices: Appendix A (worked
example of "retry lock-in" on zustand × react-expert), Appendix B (a content-driven win on pull-loading ×
js-dom-web-components), Appendix C (a cross-model sign reversal on lowdb × database-optimizer, Table 6),
Appendix D (the C3 slice-ablation protocol detail: slice definitions, pair selection, statistical tests),
and Appendix E (released artifacts).

Write ONE wiki page to:

`/Users/sergii/.kb/papers/SignalOrNoiseAgentSkills/wiki/04-appendices-worked-examples.md`

**If this file already exists (a retry), overwrite it completely.**

## Output format contract (follow exactly)

```markdown
> [[../index|Wiki]] | [[../summary|Summary]] | [[../digest|Digest]]

# Appendices: Worked Examples and Protocol Detail

**In one sentence:** <the chapter's whole argument, one sentence>

## Key points

- <5-8 bullets, each a complete standalone claim with real content — not "discusses X">

---

## Appendix A: Retry lock-in (zustand × react-expert)

<explain the mechanism concretely: the strict-mode Playwright locator collision, how C0's retry fixes it
by relabeling to "Submit" while C1's retry (anchored on the Skill's naming convention) still collides>

## Appendix B: Content-driven win (pull-loading × js-dom-web-components)

<explain: empty-wrapper visibility assertion failure, how the Skill's DOM/visibility prescription fixes it, +18pp>

## Appendix C: Cross-model sign reversal (lowdb × database-optimizer)

<reproduce Table 6 as a markdown table: Sonnet +33pp, GPT-5.1 -2pp, DeepSeek -22pp, Qwen -22pp>

## Appendix D: C3 slice-ablation protocol

<slice definitions (Rp positive rules, Rn anti-patterns, X examples), pair-selection criteria, statistical tests used (Wilcoxon signed-rank, McNemar), exact p-values reported>

## Appendix E: Released artifacts

<what was open-sourced: analysis code, routing for C1-C3, the 31 Skills with provenance, derived CSVs, repo link if given>

## Figure

![Retry lock-in and content-driven win traces](images/page9-figures1-2-retry-lockin-and-content-win.png)

<caption: 1-2 sentences based on the 09-description.md content, describing both Figure 1 (the strict-mode collision trace) and Figure 2 (the visibility-assertion fix)>

**Covers:** Appendix A, B, C, D, E
```

## Scope

Touch ONLY the one output file listed above. Do not run any fleet commands other than `bd close`.

## DoD

1. Output file written at the exact path above, following the format contract, with the figure embedded.
2. `bd close <own-id> --reason "chunk 04 extracted"`
