> [[index|Wiki]] | [[summary|Summary]]

# Connections

- [[papers/ByoCodingAgent/summary|ByoCodingAgent (byo-coding-agent)]] — Same-problem-different-language: ByoCodingAgent is the Go sibling of this course — a single `Agent` loop, a narrow provider seam, tool-pair-safe compaction, and a diff-gated TUI approval flow — teaching the identical primitive set in a single-pass ~7k-line codebase plus a bilingual course, versus this course's 15-chapter graded Python spine with a two-gate (`verify`/`accept`) acceptance rule per chapter; read together to see the same architecture taught two ways.
- [[papers/OsmaniHarness/summary|Agent Harness Engineering (Addy Osmani)]] — Applies-in-practice: Osmani's "Agent = Model + Harness" framing and context-rot defenses describe in the abstract exactly what this course implements concretely — its middle-summarizing, tool-safe `compact()` and per-item `clamp()` are working instances of the context-management primitive Osmani names, and its two-gate `verify`/`accept` rule is a concrete instance of "every failure becomes a permanent, checkable rule."
- [[papers/AmuxHarnessGuide/summary|Harness Engineering: The Complete Guide (amux.io)]] — Applies-in-practice: same Agent = Model + Harness thesis and ratchet principle, framed as a path from a lean CLAUDE.md to multi-agent orchestration; this course sits mid-path — it already has orchestration, subagents, and sandboxed execution (ch-08, ch-10, ch-11) but stops short of amux's fleet-orchestration platform scope.
- [[papers/NaturalLanguageHarnesses/summary|Natural-Language Agent Harnesses]] — Contrast-in-design: NLAH externalizes harness policy into editable natural-language documents run by a shared runtime, the opposite of this course's approach of hard-coding every primitive (compaction thresholds, approval gates, sandbox config) directly in Python modules — useful contrast when asking how much of a course-taught harness like this one could move out of code and into an editable policy document.
- [[papers/Harness/summary|Harness — Rust Control Plane for AI Agent Fleets]] — Same-primitives-different-scale: majiayu000/harness is a production-grade Rust control plane (task/turn lifecycle, policy engine, multi-agent adapters) for the same core loop this course teaches chapter by chapter; read together to see how far the teaching-scale primitives (orchestrator, subagents, verification) scale toward a fleet-grade system.
- [[papers/Superharness/summary|superharness]] — Same-problem-different-scale: Superharness coordinates several external coding-agent CLIs with a SQLite-backed queue and crash recovery, while this course's `Orchestrator`/`fan_out` (`orchestrator.py:58`, `subagents.py:30`) is the single-process, in-course version of that same plan-gate-execute-retry pattern, without persistent cross-process state.
