**Figure 9 — Factorio progress and recursive computation (two panels, shared x‑axis).**

- **X‑axis (both panels):** Cumulative output tokens generated by the root agent plus all its descendants, spanning roughly 0 to ~23M tokens over the run.
- **Panel A (top, "Technology progression"):** y‑axis = number of technologies researched (≈0–25). The curve is a monotonically non‑decreasing step function: long flat plateaus punctuated by jumps as each technology completes, climbing to about 24 completed technologies. A vertical dashed line marks a *destructive world reset* early in the run, where the count drops (≈5 → 1) before the staircase resumes. Later steps are annotated (e.g., logistic science, advanced materials, oil gathering, railway), and the final open marker indicates the run ended mid‑technology at about **71% progress on "advanced circuit."**
- **Panel B (bottom, "Agent‑tree growth and concurrency"):** left y‑axis = active (concurrently running) subagents (≈0–8); right y‑axis = cumulative subagents created (≈0–600). The orange bars show *bursty* concurrency — short, repeated waves of mostly 1–4 active subagents with occasional spikes near the max (≈7). The blue dashed line (right axis) rises near‑linearly to roughly **630+ subagents total**, confirming a wide, shallow tree rather than deep recursion.

**Trend & takeaway.** Progress is steady but intermittent: token spend accumulates smoothly while verified technology advances in discrete jumps, and the subagent population grows by repeatedly fanning out a large number of shallow (depth‑one) workers with low peak concurrency. Despite an irreversible world reset that temporarily erased most progress, the harness recovered and kept the run going, finishing with ~24 of ~196 technologies completed and ~70%+ progress on the next one — i.e., sustained, self‑organizing long‑horizon work without stalling, achieved through parallel task specialization rather than deep agent recursion.