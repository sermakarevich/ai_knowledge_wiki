# Task: Finalize wiki for SodaMem paper — verify + synthesize

This is the ONLY validation step in the whole pipeline. Follow it exactly.

## Step 1: Completeness gate (self-rearm)

List beads matching `"ArxivSodaMem chunk" extract` (`bd search` or `bd list` + grep) —
this also catches any retry beads created by an earlier finalize round. If ANY are still
open/in-progress, this run is premature: create a successor finalize bead (same spec file,
`--deps <the still-open bead ids>`), close your own bead with reason
`"rearmed as <new-id>: chunks still in flight"`, and stop.

## Step 2: Verify the wiki pages

Check these three files exist under `/Users/sergii/.kb/papers/ArxivSodaMem/wiki/`:
- `01-motivation-and-related-work.md`
- `02-method-sodamem.md`
- `03-evaluation-and-results.md`

For each: non-trivial (>40 lines), matches the format contract (backlink line,
`# Topic`, `**In one sentence:**`, `## Key points`, `---`, detail subsections,
`**Covers:**` footer), covers its assigned section of the paper (spot-check the END of
the file too, not just the opening — a known local-model failure mode is a repetition
loop that pads line count with nonsense near the end). Confirm:
- `02-method-sodamem.md` embeds `![...](images/fig1-overview.png)`.
- `03-evaluation-and-results.md` embeds `![...](images/fig2-cost-accuracy.png)` and
  reproduces Table 1 (methods sorted by accuracy) as a markdown table.

Build a BAD list and a GOOD list.

## Step 3: Handle BAD pages (if any)

If BAD is empty, go straight to Step 4.

If BAD is non-empty: the single extract bead (`ArxivSodaMem chunk 01 extract` or a retry of
it) wrote all three pages in one shot, so a bad page means the whole chunk needs a retry.
Count existing extract beads titled `"ArxivSodaMem chunk 01 extract"` (any retry suffix) to
get the attempt count.
- Attempt count < 3: delete the bad pages (or all three, to be safe), create ONE retry
  extract bead reusing `/Users/sergii/.kb/papers/ArxivSodaMem/source/specs/01-extract.md`
  verbatim (`--coder opencode --model ollama-rtx/qwen3.8:27b`), record its id, create ONE
  successor finalize bead depending on it, close your own bead with reason
  `"rearmed as <new-id>: chunk 01 requeued (attempt N)"`, and stop.
- Attempt count >= 3: this chunk has exhausted its retry budget. Write the bad page(s) by
  hand directly from `/Users/sergii/.kb/papers/ArxivSodaMem/source/chunks/01.txt` (last
  resort only). Then continue to Step 4 in this same run.

## Step 4: Synthesize the remaining artifacts

Read the wiki pages (small, not the raw source, except to spot-check quality) and produce,
per `kb show summary/get` conventions, under `/Users/sergii/.kb/papers/ArxivSodaMem/`:

- `summary.md` — rung 1 (the A2-template: Human Readable TL;DR, TL;DR, Problem & Motivation,
  Main Original Ideas, Key Findings incl. the accuracy/cost table, Suggestions & Future
  Directions, Authors & Institutions, Figures section if useful). Metadata line:
  `**Paper:** [SodaMem: Evidence-Grounded Temporal Graph Memory for LLM Agents (Wan, Wu, Lyu, 2026)](https://arxiv.org/abs/2608.08055)`.
- `digest.md` — rung 2, copying each wiki page's `**In one sentence:**` and `## Key points`
  verbatim, in order, plus a closing `## The argument in five moves`.
- `index.md` — the wiki hub: front-matter (`type: Paper`, title, description, generated by/at,
  sources: original=https://arxiv.org/abs/2608.08055 and local-copy=source/source.md, tags),
  orientation paragraph, "How to work through this" ladder, Read This Folder links, wiki
  table (3 rows, reading order), Original Source link to `source/source.md`.
- `explainer.md` — plain-language layer (80-150 lines): What is this about? / Why does it
  matter? / How does it work? / Where can this be used? / Conclusions & takeaways / Jargon
  decoder (5-12 terms, e.g. FactEvent, provenance span, supersession, BM25, connection-density
  fusion, planner-reader loop, LongMemEval).
- `questions.md` — 6-8 retrieval-practice questions (short paper, per the Scaling by source
  size table), at least one per wiki page, answers in collapsed `> [!tip]- Answer` callouts
  only — never reveal them elsewhere. Mix: recall (e.g. the 92.8% accuracy / $0.00161 figures,
  what SUPERSEDES/CONTRADICTS/UPDATES mean), elaboration (why connection-density fusion
  instead of cosine alone), transfer, and one evaluation question drawing on
  `critical_thinking.md`.
- `critical_thinking.md` — claims vs. evidence (the paper's own stated limitation: single
  store-of-record run, self-grading by the same Flash model, baseline costs reconstructed
  from disclosed tokens rather than a single-harness bake-off), genuinely new vs. repackaged
  (vs. Zep/MemGPT/Mem0/HippoRAG-style graph+temporal designs), weaknesses/blind spots,
  applicability, relevance to Sergii's work (AI/ML engineering, agentic systems, Elisity data
  platform — long-horizon agent memory, RAG evaluation), what this changes, and a verdict
  ending in adopt/trial/watch/skip with the strongest reason.
- `connections.md` — read `/Users/sergii/.kb/ai_papers/index.md` and skim
  `/Users/sergii/.kb/papers/` (recent unfiled entries) and the graph_rag category if it
  exists, for 2-6 genuinely related entries (other agentic-GraphRAG / long-term-memory papers
  ingested recently — e.g. anything about GraphRAG, HippoRAG-style memory, or agent memory
  benchmarks). If nothing is genuinely related, say so plainly rather than forcing links.

Use Obsidian `[[wikilink]]` syntax throughout; every sub-file gets a backlink line to
`index` and `summary`.

## Step 5: Report + close

Write a completion report to
`/Users/sergii/.kb/papers/ArxivSodaMem/source/delegation_report.md`: chunks total (1),
passed first try / requeued (how many rounds) / hand-written after exhausting retries.

Then: `bd close <own-bead-id> --reason "wiki complete"`.

No git commands anywhere in this task — `.kb` auto-syncs.
