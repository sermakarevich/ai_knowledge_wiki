**Figure 3 — "Visualization of the retrieved results."** This is not an axis‑based plot but a pair of node‑link (knowledge‑graph) schematics that visualize how the system's retrieval traverses an entity graph to answer a query, contrasting a wrong/failed path against a correct one.

- **Left panel (blue):** A dense hub centered on the person node *Alan Dean Foster*, connected by typed edges ("wrote", "written by", "contains", "topic", etc.) to a cluster of related entities — books (*Glory Lane*, *No Crystal Tears*, *The Deluge Drivers*), genre (*Science Fiction*), series (*Humans / Commonwealth*, *Spellfinger*), settings/topics (*silicon‑based planet*, *crystals/glass/reflective surfaces*), and dates (~1967, ~1946). A **red ✗** marker indicates this branch is the *incorrect* retrieval target (a near‑miss author with overlapping sci‑fi attributes).
- **Right panel (green):** A sparser, more focused graph centered on *Samuel R. Delany*, with nodes for identity (*Gay African American*), date (~1967), works (*The Motion of Light in Water*, *Science fiction novel*), genre (*Autobiography*), and the full‑name alias *Samuel Ray Delany Jr.*, ending in a **green ✓** marker — the *correct* target. A dashed yellow edge bridges the two panels, indicating the system moved from the near‑miss candidate to the right entity (disambiguation / graph traversal).

**Trend / takeaway:** When candidate entities are highly similar (two sci‑fi authors sharing themes and era dates), a flat retrieval would confuse them (left, ✗). The graph representation lets SAGE follow typed relationships and alias/identity cues to resolve the ambiguity and land on the correct node (right, ✓). In short, the figure illustrates that structured graph traversal improves precision in entity disambiguation over matching on overlapping surface attributes. (No numeric axes are present; the "scale" is the breadth of the wrong hub vs. the tightness of the correct path.)