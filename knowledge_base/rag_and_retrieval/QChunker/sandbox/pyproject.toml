[project]
name = "qchunker-sandbox"
version = "0.1.0"
description = "Sandbox implementation of QChunker core ideas: question-aware text chunking via multi-agent debate"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.4",
    "langchain-ollama>=0.3",
    "langchain-core>=0.3",
]

[project.optional-dependencies]
dev = ["ipython"]
